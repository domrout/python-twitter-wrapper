oauth: # Get keys by going to https://dev.twitter.com/apps/new to register application
  consumer_token: INSERT_TOKEN_HERE
  consumer_secret: INSERT_SECRET_HERE

  # The following URLs will be used by default to authenticate:
  #
  # request_token_url: http://twitter.com/oauth/request_token
  # access_token_url: http://twitter.com/oauth/access_token
  # authorize_url: http://twitter.com/oauth/authorize
